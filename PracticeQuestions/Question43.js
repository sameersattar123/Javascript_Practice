const string = "sameer Sattar"

const toCheckString = 'sa'

if (string.startsWith(toCheckString)) {
    console.warn('The string starts with "sa".');
}
else {
    console.warn(`The string does not starts with "sa".`);
}